<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="card.mng.mapper.CardMapper">

    <select id="getAllCard" resultType="cardModel">
      select
        *
      from tb_card
      where 1=1
    </select>

    <select id="getCardInfo" resultType="String">
      select
        CASE WHEN CARD_STA_CD='00' THEN '활성'
            WHEN CARD_STA_CD='01' THEN '비활성'
            WHEN CARD_STA_CD='02' THEN '분실'
            WHEN CARD_STA_CD='03' THEN '폐기'
            WHEN CARD_STA_CD='04' THEN '미발급'
            ELSE CARD_STA_CD END
      from tb_card
      where 1=1
      and card_no = #{cardNo}
    </select>

    <insert id="addCardMng">
      insert into tb_card values(#{testId}, #{testData})
    </insert>

</mapper>